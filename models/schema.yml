version: 2

sources:
  - name: raw
    schema: gz_raw_data
    tables:
      - name: product
        identifier: aw_gz_product
        tests:
          - unique
      - name: sales
        identifier: raw_gz_sales
      - name: ship
        identifier: raw_gz_ship
